@model MyApp.Web.Portal.ViewModels.LoginViewModel
@{
    ViewBag.Title = "Iniciar sesión";
}
<section id="loginForm">
    @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { role = "form", autocomplete = "off" }))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(excludePropertyErrors: false)
        <div class="form-group">
            @Html.LabelFor(m => m.Email, new { @class = "control-label" })
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Password, new { @class = "control-label" })
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
        </div>
        <div class="row">
            <div class="col-5">
                <div class="custom-control custom-checkbox">
                    @Html.CheckBoxFor(m => m.RememberMe, new { @class = "custom-control-input" })
                    <label class="custom-control-label" for="@Html.IdFor(m => m.RememberMe)">Recordarme</label>
                </div>
            </div>
            <div class="col-7 text-right">
                @Html.ActionLink("¿Olvidaste tu contraseña?", "OlvidoPassword", "Account")
            </div>
        </div>
        <div class="row mt-2">
            <div class="col">
                <input type="submit" class="btn btn-primary" value="Ingresar" />
            </div>
        </div>
    }
</section>
