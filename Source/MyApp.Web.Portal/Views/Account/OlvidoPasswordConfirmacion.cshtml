@{
    ViewBag.Title = "Olvidé mi contraseña";
}
<section id="loginForm">
    <div class="mt-3">
        @Html.Partial("_MensajeOk", "Se ha enviado el correo para restablecer tu contraseña")
    </div>
    <p class="mt-3">
        <a href="@Url.Action("Login", "Account")">Volver</a>
    </p>
</section>
